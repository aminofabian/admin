/**
 * @deprecated Import from `@/hooks/use-chat-websocket` instead.
 * Shim so legacy relative imports do not duplicate chatroom-id / abort logic.
 */
export { useChatWebSocket } from '@/hooks/use-chat-websocket';
